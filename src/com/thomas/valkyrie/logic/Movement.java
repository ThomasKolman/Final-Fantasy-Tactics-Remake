package com.thomas.valkyrie.logic;

/**
 * Created by Thomas on 2016-01-18.
 */
public class Movement
{
    private Byte[][] grid = new Byte[9][9];

    public static void checkMovementSpace(int mobility)
    {
        int xPos = 0;
        int yPos = 0;

        for (int i = 0; i <= mobility; i++)
        {

        }
    }

//    private int calculateHeuristic()
//    {
//        // Calculates the Manhattan distance between starting point and target point
//        return Math.abs(xPos - targetXPos) + Math.abs(yPos - targetYPos);
//    }
}
